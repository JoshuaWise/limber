fix up template;
make sure the p and hx base styles are how I want them (em? inherit? etc.)
also what I want to do with "font-scale-" in site-globals.styl and validate-globals.styl

add grid module

make sure outdated browser test accounts for everything used in framework
(use modernizr?)
(make sure browser can handle 'use strict')

confirm full framework

--------------------------------------------------------------------------------
Supported browsers:
"
	chrome >= 31,
	ff >= 31,
	safari >= 7,
	ie >= 11,
	edge >= 1,
	opera >= 28,
	and_chr >= 44,
	and_ff >= 40,
	android >= 40,
	ios >= 8,
	ie_mob >= 11,
	op_mob >= 30
"
--------------------------------------------------------------------------------
When Stylus 1.0 is released:
  - add newly required semicolons to all properties
  - use proper arrays instead of lists
  - put all limber variables in a namespaced hash
  